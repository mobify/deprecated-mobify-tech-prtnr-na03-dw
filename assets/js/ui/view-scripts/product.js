define([
    '$'
],
function($) {
    var productUI = function() {
        // Add any scripts you would like to run only on the product page here
        $('.m-scooch').scooch();
    };

    return productUI;
});
