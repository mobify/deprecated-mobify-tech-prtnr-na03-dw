define([
    '$'
],
function($) {
    var cartUI = function() {
        // Add any scripts you would like to run only on the cart page here
        console.log('cart');
    };

    return cartUI;
});
